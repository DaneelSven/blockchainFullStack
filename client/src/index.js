import React from "react";
import { render } from "react-dom"; // use curly braces if it is not the default export
import App from "./components/App";
import "./index.css";

render(<App />, document.getElementById("root"));
